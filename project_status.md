# 七夕線上煙火大會 - 專案狀態與檢討報告

## 1. 專案目標與核心訴求
本專案旨在為七夕情人節打造一個「輕量級、免註冊」的雙人互動網頁，讓使用者能透過簡單的連結與另一半在線上同步欣賞煙火。

**核心目標：**
- 雙人即時互動（點擊施放煙火）。
- 溫馨可愛的主視覺（倉鼠與卡皮巴拉的背影）。
- 提供不同的視覺風格切換（動畫風 vs 像素風）。
- 良好的響應式體驗（手機與電腦皆可完美呈現）。

## 2. 專案系統架構
- **前端 (Frontend)**：
  - **HTML/CSS**：負責介面排版、登入對話框、提示訊息。
  - **Vanilla JavaScript**：控制介面邏輯、與後端建立 Socket 連線。
  - **HTML5 Canvas**：繪製高效能的煙火粒子系統（包含自定義的像素方塊特效與重力物理）。
- **後端 (Backend)**：
  - **Node.js + Express**：提供靜態網頁檔案的伺服器。
  - **Socket.IO**：負責建立 WebSocket 即時連線，處理使用者加入、廣播煙火座標與風格切換事件。

---

## 3. 目前未達預期之處 (Unmet Expectations)

根據您的回饋，目前有以下幾個問題未能達到完美：

### ❌ 響應式設計 (RWD) 失敗
- **預期**：在電腦 (16:9) 與手機 (9:16) 上，都能呈現「大片天空在上方、兩隻角色在下方正中央」的水平視角。
- **現狀**：您反映響應式效果並未出來。這可能是因為：
  1. CSS 的 `object-fit: cover` 在不同裝置的縮放行為不如預期，導致角色被切斷或比例跑掉。
  2. 瀏覽器快取 (Cache) 導致您可能還在看舊版的畫面（未吃到我第二次修改的 CSS/JS）。
  3. 將「角色」與「天空」壓在同一張圖裡，很難在極端的寬螢幕與長螢幕間完美適應。若要做到完美，**必須將「天空背景」與「前景角色」拆分成兩張獨立的透明圖層**。

### ❌ 風格切換未生效 (背景圖相同)
- **預期**：點擊切換風格時，背景會從「動畫風」切換成「像素風」。
- **現狀**：您反映兩個風格的背景圖是一樣的。這同樣極大機率是因為：
  1. 瀏覽器強烈快取了 `index.html` 與 `script.js`，導致 JavaScript 中切換 `<img>` src 的邏輯沒有執行，或載入了舊的邏輯。
  2. 圖片檔案的讀取路徑有誤。

### ❌ 角色位置與名字浮動的精準度
- **預期**：倉鼠跟卡皮巴拉肩並肩，且名字精準地浮在兩者頭上。
- **現狀**：因為目前使用的是單一張「AI 生成的靜態圖」，兩隻角色在不同風格圖片中的相對位置並不固定（有時左邊是倉鼠，有時是卡皮巴拉）。這導致我們只能用寫死的 CSS 百分比去「猜」名字的位置。一旦螢幕比例改變，名字就會飄走。

---

## 4. 下一步修正計畫 (Action Items)

為了徹底解決上述問題，我建議採取以下做法：

1. **強制清除快取 / 引入快取破解 (Cache Busting)**：
   - 確保下次修改時，程式碼會被強制重新載入，避免看錯版本。
2. **重構視覺圖層 (Layer Separation)**：
   - 放棄使用一整張圖片。我們需要將畫面分為 **「獨立的星空背景圖」** + **「獨立的倉鼠與卡皮巴拉前景圖 (透明 PNG)」**。
   - 這樣我們可以用 CSS 將背景設為 100% 覆蓋天空，而角色前景則永遠使用 `bottom: 0; left: 50%; transform: translateX(-50%);` 固定在畫面正下方。這能 100% 保證響應式完美。
3. **動態綁定名字座標**：
   - 若角色是獨立的 HTML 元素，我們就能直接將名字附加在角色元素的上方，再也不用擔心螢幕縮放導致名字對不準頭部。

> 請您檢閱這份狀態報告，若同意我朝**「拆分背景與角色圖層」**的方向重構以解決響應式與對齊問題，我將立即為您尋找或生成透明去背的角色素材並修改程式碼！

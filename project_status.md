# 七夕線上煙火大會 — 專案狀態報告

> 本檔案先前混入 UTF-16 位元組導致第 5 節顯示為亂碼，已重寫為純 UTF-8。

## 1. 專案目標

為七夕打造「輕量級、免註冊」的雙人互動網頁，透過一組房間密碼與另一半同步欣賞煙火。

- 雙人即時互動（點擊施放煙火）
- 溫馨可愛的主視覺（倉鼠與卡皮巴拉的背影）
- 動畫風 / 像素風雙視覺切換
- 手機與電腦皆完美呈現

## 2. 系統架構

```
public/
  index.html          介面骨架（__BUILD__ 由伺服器注入）
  css/style.css
  js/
    scene.js          場景資料 + cover 投影 + 錨點計算   ← 核心
    fx.js             煙火引擎（粒子池、火箭、造型、雙美術風格）
    audio.js          WebAudio 合成音效（零素材）
    ui.js             名牌、對話泡泡、IME 安全輸入
    main.js           組裝與 Socket 事件
  assets/             4 張使用中的背景圖
server.js             Express + Socket.IO，含驗證與速率限制
```

## 3. 原本三個「未達預期」問題 — 根因與修正

### RWD 失敗
**真因不是 CSS，也不是快取。** 橫式與直式的美術圖中，兩隻角色的**左右位置是相反的**：

| 圖檔 | 左 | 右 |
|---|---|---|
| `anime_16x9` / `pixel_16x9` | 倉鼠 | 卡皮巴拉 |
| `anime_9x16` / `pixel_9x16` | **卡皮巴拉** | **倉鼠** |

舊的 `getCharacterPositions()` 兩種情況都假設 index 0 = 倉鼠在左，所以直式手機必然錯位。且 `12vw` / `18vh` 這類寫死值與 `object-fit: cover` 的實際縮放沒有數學關係。

**修正：圖片座標系投影（Image-Space Anchor Projection）**
每個角色的錨點以「原圖的 0..1 座標」記錄在 `scene.js`，前端用 JS 重現 cover 變換後投影成螢幕像素。任何螢幕比例都精準。

同時**棄用 CSS `object-fit`**，改由 JS 直接設定 `<img>` 的 width/height/translate — 避免 CSS 與 JS 各持一套變換而靜默飄移。

### 風格切換未生效
**真因是 `<picture>` 規格行為，不是快取。** 首次 layout 後再改 `<source srcset>`，各瀏覽器重新評估行為不一致，iOS Safari 常沿用舊圖 — 所以清快取也沒用。

**修正：** 改用雙層 `<img>` + JS `matchMedia` 主動選圖 + 交叉淡入。

### 名字浮動精準度
名牌與對話泡泡皆綁定投影後的錨點。實測名牌水平中心與錨點誤差 < 0.1px，底邊恰好貼齊頭頂。

## 4. 視覺升級（藝術總監視角）

| 項目 | 說明 |
|---|---|
| 火箭升空 | 煙火自地面升起、拖曳火星、抵達頂點才爆炸 |
| 5 種爆炸造型 | peony / willow / ring / chrysanthemum / **heart**（七夕專屬，另有 💗 按鈕） |
| 立體感 | 球面取樣，以 z 分量驅動粒子大小與亮度 |
| 輝光 | 離屏 radial-gradient sprite + `lighter` 疊加（非 `shadowBlur`，快約 10 倍） |
| 爆炸閃光 | 每次爆炸在畫面投下光暈 |
| 地面光溢散 | `#ground-glow` 以 `mix-blend-mode: screen` 讓草地被煙火照亮 |
| 前景景深 | `#foreground-scrim` 讓落入草叢的火花沉入陰影，無需去背素材 |
| 流星 | 天空隨機掠過 |
| **殘煙** | 爆炸後留下 11 團不規則低透明度煙霧，緩慢上升擴散（僅動畫風；柔霧會破壞像素美術）|
| **Bloom 輝光** | 將完成的畫面降採樣至 1/4 後模糊，以 `screen` 疊回。動畫風 0.85、像素風 0.4（後者像 CRT 螢光殘影）|
| **視差** | 背景隨指標或裝置傾斜位移 ±11px。**位移併入 `stage.rect`**，所以名牌與泡泡跟著移動，不會脫離角色 |
| **螢火蟲** | 草地上 8–22 隻閃爍飄移。閃爍用 sin³ 產生銳利脈衝與長暗期，單純 sin 只會呆板地明滅 |
| 雙美術風格 | 動畫風＝加法混色輝光；像素風＝`source-over` 硬邊方塊、色階量化、網格對齊 |

### 視覺調校紀錄（實際看圖後修正）

初版有兩個只有看到畫面才會發現的缺陷：

1. **加法混色過曝** — 粒子貼圖的白色核心過大，疊加後全部飽和成純白，粉紅與金色只剩邊緣殘留。已將白核縮到半徑 6%、每顆粒子 alpha 降至 0.6。
2. **爆炸尺寸過大** — 原本 `BURST_REACH = 0.32`，一朵煙火橫跨半個畫面、蓋住角色。已降至 `0.16`，並將閃光尺寸改為以爆炸半徑為基準而非整個視窗（原本像一團霧氣蓋住火花）。

另外把拖尾衰減 `fade` 從 3.4 提高到 5.2 —— 太慢會讓下墜的火星拖成長條光柱，看起來像下雨而不是煙火。

3. **畫面下半部的霧影（Canvas 精度 bug）** — 拖尾靠每幀 `destination-out` 淡出前一幀，但**這個淡出永遠到不了零**：它算的是 `alpha × (1 - a)`，在 8-bit 整數下 `1 × 0.95 = 0.95` 會四捨五入回 `1`，就此卡死。螢火蟲每幀都畫在同一條草地帶，那個殘留下限便累積成一片永久霧氣，bloom 再把它模糊放大。像素風特別嚴重，因為像素螢火蟲是**全不透明**方塊，印得更死。

   實測下半部殘留像素：0s → 6s → 12s 為 **2,528 → 8,709 → 14,964**，單調累積不會消失。

   修正：只有煙火元素需要拖尾，螢火蟲每幀都重畫、根本不需要。因此在沒有任何煙火元素在場時改為 `clearRect` 硬清（`_fadeOrClear`）。安靜的空檔很頻繁，畫布等於持續自我清潔。修正後同樣量測為 **0 → 0 → 4**，演出結束 5 秒後回到 0。

   > 註：先前誤判為 `#foreground-scrim`（那層確實也該移除，它讓草地變濁），但真正的霧影來源是這個精度問題。

4. **地面反光的長方形邊界** — `#ground-glow` 是一個 `inset: 0` 的 DOM 層，用 radial-gradient 當背景、`mix-blend-mode: screen` 疊上去。合成後**元素本身的方框邊界會顯現**，在畫面下半部形成一條長方形界線。

   修正：改由 canvas 繪製（`_stepGroundLight`）。光暈用既有的柔邊 sprite，其 alpha 在繪製矩形內就已歸零，因此沒有方框可看。量測整個畫面每一列的平均 alpha，最大相鄰列跳變為 **0.11**（方框邊界會是斷崖），確認為平滑漸層。

   額外好處：畫在 canvas 上就能被擷取驗證，不再是我看不到的圖層。

### 音效重寫

原本的爆炸只是「正弦波下滑 + 濾波雜訊」，聽起來像鼓而不像煙火。真實的煙火是三層：

| 層 | 內容 |
|---|---|
| **Crack** | 極短的高頻瞬態（~55ms），這才是讓耳朵判定「爆炸」的關鍵 |
| **Boom** | 低頻本體，用**濾波雜訊**而非純音（純音會聽起來像樂器），截止頻率由 420Hz 掃到 90Hz |
| **Crackle** | 26–44 顆隨機散佈在後續 1.3 秒的細小爆音 —— 最具辨識度、原本完全沒有 |

全部經過程式生成的 convolution reverb（雜訊指數衰減脈衝響應，零素材），讓聲音像發生在幾百公尺外的開闊夜空，而不是在耳朵裡。

以 OfflineAudioContext 離線算出波形驗證包絡：0–50ms 峰值 0.242（crack）→ 50–200ms 衰減（boom）→ 0.2–1.3s 不規則 0.001–0.028（crackle）→ 1.45s 歸零。峰值未破音。
| 角色染色 | 倉鼠暖色系、卡皮巴拉冷色系 — 一眼看出是誰放的 |
| 音效 | WebAudio 合成升空與爆炸聲，**預設靜音**，按鈕提示一次 |

## 4.5 房號碰撞與隱私（重要）

**房號就是密碼，而且是唯一的一道門。** 實測兩對情侶都用 `0819`：

| 對象 | 結果 |
|---|---|
| 阿明（A 組） | 進入房間 |
| 小美（B 組陌生人） | **也進入同一間** |
| 阿明的女友（A 組） | ❌ 被鎖在門外 |
| 阿明傳的私訊 | **小美收到了** |

情侶被拆散、私訊外洩。已採取的對策：

1. **🎲 產生專屬房號** — 6 碼、31 字元表（排除 `0O1IL` 等易混字），約 8.87 億組合。碰撞機率實務上為零。
2. **邀請連結** — 進入後左上角可複製 `?room=XXXX` 連結，另一半點開自動帶入房號，不需口述。
3. **提示文字** — 登入頁明講「太好猜的房號可能會撞房」。
4. **加快幽靈席位釋放** — Socket.IO ping 由預設 25s/20s 調成 10s/8s，最壞情況從約 45 秒縮短到約 18 秒；正常關閉則 0.5 秒內釋放。

**殘留風險：** 使用者若堅持自己輸入 `0819`，仍然會撞房 —— 這是「共用密碼」的本質，不是 bug。若要根除，需要引入獨立於房號的配對驗證（例如建立者核准加入），屬於更大的改動。

## 5. 待辦：圖層完全分離（第二階段，維持原決策）

- **目標**：火花能在角色與草地「後方」爆炸的真實遮擋感。
- **需要**：將天空挖空、存成透明 PNG 的角色前景素材（需人工去背）。
- **做法**：CSS 星空漸層背景 → 煙火 Canvas → 透明角色前景 PNG 三層堆疊。
- **現況替代方案**：已用 `#foreground-scrim` 景深 + 火花沉入地平線做出近似效果，無需新素材。
- **狀態**：保留至後續階段。

## 6. 已知事項

- `public/assets/bg_anime.jpg` 與 `bg_pixel.jpg`（合計約 1.9MB）**未被任何程式碼引用**，屬死素材。確認後可刪除：
  ```bash
  git rm public/assets/bg_anime.jpg public/assets/bg_pixel.jpg
  ```
- 修改 `index.html` 後需**重啟伺服器**（其內容於啟動時讀入記憶體並注入 build id）。
- 錨點座標為目視量測。若需微調，開啟 `?debug=1`：畫面會顯示十字準星與地平線，`Tab` 切換角色、方向鍵微調、`Shift` 加速，新數值會印在 console，再回填 `js/scene.js` 即可。
